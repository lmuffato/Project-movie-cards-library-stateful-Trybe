import React from 'react';

class SearchBar extends React.Component {
  render() {
    return (
      <div>
        Filtrar por gênero
        <select>
          <option>Ação</option>
        </select>
      </div>
    );
  }
}

export default SearchBar;
